const state = {
  test: null,
};

const getters = {
  test: (state) => {
    return state.test
  },
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};